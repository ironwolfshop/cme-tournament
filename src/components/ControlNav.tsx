/**
 * @deprecated Prefer StudioShell / StudioSidebar for control desks.
 * Kept as a thin compatibility shim for any remaining imports.
 */
export { StudioSidebar as default } from './cme/StudioShell'
